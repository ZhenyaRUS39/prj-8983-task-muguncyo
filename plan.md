# Plan: prj-8983-task-muguncyo

**Project:** `PRJ-1345`  
**Task ID:** `task-muguncyo`  
**Repo:** `prj-8983-task-muguncyo`  

## Summary

Декомпозиция задачи "Создай GraphQL API на Apollo Server" на 4 последовательных этапа: scaffold проекта → схема + резолверы + тесты → авторизация и валидация → интеграция с БД и финальная отделка.

## Subtasks

### 1. Scaffold Node.js проекта с Apollo Server

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Инициализировать npm-проект, установить apollo-server, graphql, typescript, jest. Настроить tsconfig, базовый src/index.ts с запуском ApolloServer на порту 4000, package.json со скриптами dev/start/test, .gitignore, README.md с инструкцией запуска.
- **Test plan:** Запустить npm run dev, открыть http://localhost:4000 — должна открыться GraphQL Sandbox/Sandbox стартовая страница; npm run build не должен падать; package.json содержит скрипты dev/start/test.

### 2. Реализовать GraphQL схему и резолверы + unit-тесты

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Описать SDL-схему (typeDefs) с типами User {id, name, email} и Query {users: [User], user(id: ID!): User}; реализовать резолверы с in-memory массивом мок-данных (3-5 user). Написать jest-тесты, проверяющие что Query.users возвращает массив длины >=3, Query.user возвращает корректного пользователя по id и null для несуществующего.
- **Test plan:** npm test проходит все тесты зелёным; npm run dev + GraphQL запрос {users{id,name,email}} возвращает массив из 3+ объектов с непустыми полями.
- **Dependencies:** `sub-1`

### 3. Добавить валидацию входных данных и обработку ошибок

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить поле Mutation с createUser(name: String!, email: String!): User с валидацией email (regex) и длины name (>=2). Реализовать typed GraphQL ошибки через GraphQLError. Написать jest-тесты: успешное создание, ошибка при невалидном email, ошибка при коротком name.
- **Test plan:** npm test — все тесты зелёные; GraphQL мутация createUser с email=test даёт ошибку валидации; createUser с валидными данными возвращает созданного User с присвоенным id.
- **Dependencies:** `sub-2`

### 4. Подключить persistence-слой (SQLite) и финальные E2E тесты

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить better-sqlite3 (или sqlite3) для хранения users в БД, заменить in-memory store на DB. Написать миграцию для таблицы users, seed-скрипт. E2E jest-тест: поднять ApolloServer, выполнить через executeOperation createUser → users, проверить что данные сохранились между запросами. Обновить README с примерами запросов.
- **Test plan:** npm run build без ошибок; npm test покрывает E2E через executeOperation; в README есть примеры query users/createUser; после рестарта сервера данные пользователей не теряются (SQLite файл сохраняется на диск).
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
